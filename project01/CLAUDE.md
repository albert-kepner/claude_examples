# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is a simple Python 3.13 project managed with uv (a fast Python package manager). The project currently contains a basic entry point in `main.py`.

## Development Commands

```bash
# Run the main script
python main.py

# Or using uv
uv run main.py
```

## Project Configuration

- Python version: 3.13 (specified in `.python-version`)
- Project metadata is defined in `pyproject.toml`
- No external dependencies currently configured

## Code Style

- **Indentation**: Always use spaces for indentation, never tabs. Follow PEP 8 standard of 4 spaces per indentation level.
